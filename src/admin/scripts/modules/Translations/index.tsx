import React from 'react';

interface TranslationsProps {}

const Translations = ({}: TranslationsProps) => {
	return (
		<>
			<div>Translations list</div>
			<div>Translations detail</div>
		</>
	);
};

export default Translations;
